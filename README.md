# 🕷️ Web Parser

Веб-приложение для парсинга сайтов в JSON. Умеет парсить как обычные статические сайты, так и SPA (React, Vue, Angular) — благодаря Puppeteer, который рендерит JavaScript перед извлечением данных.

## 🚀 Демо

- **Render:** `https://web-parser-sgbw.onrender.com/`
- **Railway:** `web-parser-production.up.railway.app`

## 🛠️ Стек

| Часть | Технология |
|-------|-----------|
| Frontend | React 18 + Vite |
| Backend | Express.js |
| SPA-парсинг | Puppeteer Core + системный Chromium |
| HTML-парсинг | Cheerio |
| HTTP-запросы | Axios |

## 📁 Структура проекта

```
web-parser/
├── client/              # React приложение (Vite)
│   ├── src/
│   │   ├── App.jsx      # Главный компонент
│   │   └── main.jsx     # Точка входа
│   ├── index.html
│   └── package.json
├── server/
│   └── index.js         # Express сервер + логика парсинга
├── render.yaml          # Конфиг деплоя на Render
├── package.json         # Корневой package.json
└── README.md
```

## ⚙️ Как это работает

1. Пользователь вводит URL сайта
2. Сервер делает быстрый запрос через Axios и смотрит — есть ли контент в `<body>`
3. Если `body` почти пустой (< 200 символов) — сайт определяется как **SPA** и парсится через **Puppeteer** (headless Chrome, ждёт выполнения JS)
4. Если контент есть — парсится быстро через **Axios + Cheerio**
5. Результат возвращается как JSON: заголовки, параграфы, ссылки, изображения, мета-теги

## 🖥️ Локальный запуск

### Требования

- Node.js >= 20
- npm >= 9

### Установка и запуск

```bash
# 1. Клонируй репозиторий
git clone https://github.com/ВАШ_НИК/web-parser.git
cd web-parser

# 2. Установи серверные зависимости
npm install

# 3. Установи клиентские зависимости
cd client && npm install && cd ..

# 4. Запусти сервер (порт 3001)
node server/index.js

# 5. В отдельном терминале запусти клиент (порт 5173)
cd client && npm run dev
```

Открой `http://localhost:5173` в браузере.

> ⚠️ Если используешь MetaMask или другие Web3 расширения — открывай в режиме **инкогнито**, иначе Vite не загрузится.

## 🌐 Деплой

### Render (рекомендуется)

1. Зарегистрируйся на [render.com](https://render.com)
2. **New → Web Service → Connect GitHub repo**
3. Render автоматически подхватит `render.yaml`
4. В **Settings → Environment** добавь переменную: `NODE_VERSION = 20.11.0`
5. Нажми **Deploy**

Билд занимает ~5-7 минут (устанавливается Chromium).

### Railway

1. Зарегистрируйся на [railway.app](https://railway.app)
2. **New Project → Deploy from GitHub**
3. Выбери репозиторий
4. В **Settings** укажи: Build Command: `npm run build`, Start Command: `npm start`
5. В **Variables** добавь: `NODE_VERSION = 20.11.0`

### Переменные окружения

| Переменная | Значение | Описание |
|-----------|---------|---------|
| `NODE_VERSION` | `20.11.0` | Версия Node.js |
| `CHROMIUM_PATH` | `/usr/bin/chromium` | Путь к Chromium на сервере |
| `PORT` | `3001` | Порт сервера (Render подставляет автоматически) |

## 📊 Что парсит

- **title** — заголовок страницы
- **meta** — все мета-теги (description, og:title, og:image и др.)
- **headings** — все заголовки h1-h6
- **paragraphs** — текстовые параграфы (до 20)
- **links** — все ссылки с текстом (до 50)
- **images** — все изображения с alt-текстом (до 20)
- **renderMethod** — каким методом был спарсен сайт (axios / puppeteer)

## 🧪 Тестовые сайты

| Сайт | Тип | Ожидаемый метод |
|------|-----|----------------|
| `https://example.com` | Статический | axios |
| `https://react.dev` | React SPA | puppeteer |
| `https://vercel.com` | Next.js | puppeteer |
| `https://excalidraw.com` | React SPA | puppeteer |

## 📝 API

### `POST /api/parse`

**Request:**
```json
{
  "url": "https://example.com"
}
```

**Response:**
```json
{
  "url": "https://example.com",
  "title": "Example Domain",
  "meta": { "description": "..." },
  "headings": [{ "tag": "h1", "text": "Example Domain" }],
  "paragraphs": ["This domain is for use in..."],
  "links": [{ "href": "https://...", "text": "More information" }],
  "images": [{ "src": "/logo.png", "alt": "Logo" }],
  "renderMethod": "axios (SSR/static)",
  "parsedAt": "2026-02-26T12:00:00.000Z"
}
```

## 📄 Лицензия

MIT
